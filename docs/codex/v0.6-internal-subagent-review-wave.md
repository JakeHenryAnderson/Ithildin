# v0.6 Internal Subagent Review Wave

This note records the internal high-intelligence subagent pressure-test wave that followed the v0.6
dispatch packet preparation. It is internal review evidence only. It does not close external/source
review rows, does not approve new tool powers, and does not change the v0.1 local-preview runtime
boundary.

## Review Lanes

| Lane | Reviewer label | Scope | Result |
| --- | --- | --- | --- |
| Patch apply | Russell | `fs.patch.apply` approval binding, stored-proposal-only apply, attempt diagnostics, and dispatch packet pointers | No implementation blocker found; one documentation pointer drift recorded as SUB-002. |
| Filesystem/platform | Ramanujan | `fs.*`, `git.*`, path/race contract claims, patch replace assumptions | Two high and two medium internal findings recorded as SUB-003 through SUB-006. |
| HTTP fetch | Kepler and follow-up proxy reviewers | `http.fetch` URL parsing, allowlist, DNS/IP/redirect handling, safe errors, audit resource evidence, preview parity, and packet traceability | Original findings SUB-007 through SUB-009 plus follow-up findings SUB-040 through SUB-047 are fixed internally; external review remains pending. |
| Audit and signed evidence | Curie plus follow-up proxy reviewers | signed audit export lifecycle, offline verification, signed manifest-lock status, demo evidence, and release packet traceability | Original findings SUB-010 through SUB-014 plus follow-up findings SUB-048 through SUB-063 are fixed internally; external review remains pending. |
| Policy/registry | Descartes, Hegel, and Dirac | policy preview/runtime parity, trusted principal resolution, registry fail-closed behavior, manifest drift, YAML parsing, and dispatch traceability | Original findings SUB-015 through SUB-017 plus follow-up findings SUB-064 through SUB-073 are fixed internally; external review remains pending. |
| MCP ingress | Euler and follow-up proxy reviewer | stdio adapter thinness, fixed principal/session, MCP exposure enforcement, and denial evidence | Original finding SUB-018 plus follow-up finding SUB-074 are fixed internally; external review remains pending. |
| Review console/admin | Mencius and follow-up proxy reviewer | approval review evidence, local admin mutation routes, trust posture, copied evidence, and failure states | Original findings SUB-019 through SUB-021 plus follow-up finding SUB-075 are fixed internally; external review remains pending. |

## Findings

- SUB-002: patch apply dispatch/checklist pointer drift.
- SUB-003: patch apply ancestor-directory symlink swap before atomic replace.
- SUB-004: `git.diff` hidden/sensitive tracked-path leak.
- SUB-005: symlinked directory component accepted when it resolves inside the workspace.
- SUB-006: NUL/control path input can escape as raw path errors.
- SUB-007: HTTP post-open response processing errors can surface raw exception details.
- SUB-008: explicit empty or zero ports can normalize ambiguously.
- SUB-009: HTTP audit resources can include query-string secrets.
- SUB-040: malformed HTTP URL resources can retain raw query/userinfo secrets.
- SUB-041: HTTP preview could construct network resources before schema validation.
- SUB-042: raw non-ASCII HTTP path/query could escape safe transport handling.
- SUB-043: JSON parser edge failures could escape response bounds handling.
- SUB-044: HTTP dispatch packet omitted several internal finding records.
- SUB-045: HTTP closure traceability lacked fixed-commit/test specificity.
- SUB-046: HTTP lane lacked a lane-level result and residual-risk note.
- SUB-047: HTTP runbook contract link pointed at a stale filename.
- SUB-048: failed signed export attempts could write successful export evidence.
- SUB-049: re-signed bundles could alter nested export metadata not checked by verification.
- SUB-050: signed audit export docs/CLI could imply embedded-key self-verification.
- SUB-051: non-object signed export bundles could crash verification paths.
- SUB-052: corrupt audit payloads could return plain server errors from audit listing.
- SUB-053: manifest-lock signature status could leak lock-read failures out of optional status paths.
- SUB-054: release evidence could pass while signed manifest-lock enforcement was required but unverified.
- SUB-055: `/system/status` recomputed signed-lock status without startup evidence.
- SUB-056: failed manifest-lock signature status could surface untrusted key IDs.
- SUB-057: signed-evidence demo verification ignored a custom lock path.
- SUB-058: signed-evidence closure rows were stale relative to internal findings.
- SUB-059: signed-evidence dispatch packets had stale pointers and incomplete finding coverage.
- SUB-060: generated review artifacts needed regeneration after remediation.
- SUB-061: signed-evidence demo verification lacked a durable bundle transcript.
- SUB-062: release-automation dispatch packets omitted release-evidence commands.
- SUB-063: release evidence accepted a passing transcript without explicit return-code evidence.
- SUB-064: explicit empty preview principals could default to admin.
- SUB-065: patch-apply parity omitted the real patch proposal service.
- SUB-066: JSON Schema validation errors could echo caller values.
- SUB-067: pre-policy preview denials looked too much like policy decisions.
- SUB-068: strict workspace registry mode could fall back to an overridden root.
- SUB-069: `/system/status` did not report current manifest-lock drift.
- SUB-070: duplicate YAML keys were accepted in trusted config.
- SUB-071: policy/registry findings were missing from closure and dispatch traceability.
- SUB-072: policy/registry dispatch packets lacked source/command coverage.
- SUB-073: policy/registry lane result evidence was missing.
- SUB-074: MCP registered-but-unexposed call denials bypassed shared audit evidence.
- SUB-075: review-console copied approval evidence omitted derived review verdict/checks/reasons.

## Remediation Status

The findings are recorded in `docs/codex/findings/` using the shared reviewer finding format. The
current remediation pass fixes each finding internally and leaves external/source review pending.
The source-review closure matrix remains external-pending for all affected areas.

## Filesystem Recheck Follow-Up

A later v0.6 proxy-review pass rechecked the filesystem/platform lane after the patch-apply
external-finding remediation. It recorded and fixed `SUB-032` through `SUB-039`, covering read-side
ancestor symlink races, path control-character filtering, list/search symlink traversal, filesystem
capability evidence, patch output text-only guarantees, pre-replace stale-base checks, and invalid
Unicode patch denial. This remains internal proxy evidence only; external/source review is still
pending.

## Policy/Registry Recheck Follow-Up

A later v0.6 proxy-review pass rechecked the policy/registry lane. It recorded and fixed `SUB-064`
through `SUB-073`, covering explicit principal handling, real patch-apply parity service coverage,
safe validation errors, pre-policy denial evidence, strict workspace registries, current manifest
drift status, duplicate YAML key rejection, dispatch coverage, and lane result traceability. This
remains internal proxy evidence only; external/source review is still pending.

## MCP Ingress Recheck Follow-Up

A later v0.6 proxy-review pass rechecked the MCP ingress lane. It recorded and fixed `SUB-074`,
covering the audit-evidence gap for registered-but-unexposed MCP calls. These calls remain denied
before execution and now record safe shared denial evidence. This remains internal proxy evidence
only; external/source review is still pending.

## Review Console/Admin Recheck Follow-Up

A later v0.6 proxy-review pass rechecked the review console/admin lane. It recorded and fixed
`SUB-075`, covering copied approval evidence parity with the visible approval-review verdict. This
remains internal proxy evidence only; external/source review is still pending.

## Boundary

This wave added no new MCP tools, manifests, policy powers, identity model, storage backend, MCP
transport, shell/Docker/Kubernetes/browser capability, arbitrary HTTP behavior, or broad filesystem
write path. It tightened the existing local-preview mediation boundary.

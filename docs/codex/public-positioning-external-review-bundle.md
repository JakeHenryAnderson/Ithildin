# Public Positioning External Review Bundle

Status: reviewer launch bundle for blocked `ERG-010`.

Current governed tool count: `24`.

Current `ERG-010` status before reviewer disposition: `blocked`.

This document describes the focused external-review bundle generated by:

```sh
make public-positioning-external-review-bundle
```

Validate the bundle wiring with:

```sh
make public-positioning-external-review-bundle-check
```

The generated bundle lives under:

```text
var/review-packets/v3/public-positioning-external-review/
```

## Purpose

The bundle gives a reviewer one focused handoff for the `ERG-010` public/security-product
positioning and claim-boundary lane. It consolidates:

- the public/security-product positioning decision intake;
- the fail-closed public positioning closure gate;
- the residual docs/claims public-preview closure gate;
- current v0.8 and v1.0 local-preview decision evidence;
- accepted-risk and enterprise queue context;
- blocked-claim and non-goal evidence;
- command evidence and artifact hashes.

## Artifacts

The bundle generates:

1. `00_PUBLIC_POSITIONING_EXTERNAL_REVIEW_INDEX.md`
2. `01_PUBLIC_POSITIONING_EXTERNAL_REVIEW_PROMPT.md`
3. `02_PUBLIC_POSITIONING_DECISION_INTAKE.md`
4. `03_PUBLIC_POSITIONING_CLOSURE_GATES.md`
5. `04_PUBLIC_POSITIONING_CLAIM_EVIDENCE.md`
6. `05_PUBLIC_POSITIONING_REPRODUCTION_QUEUE_STATUS.md`
7. `06_PUBLIC_POSITIONING_BOUNDARY_EVIDENCE.md`
8. `07_PUBLIC_POSITIONING_COMMAND_EVIDENCE.md`
9. `public-positioning-external-review-artifact-hashes.json`

## Boundary

This bundle does not prove external review has happened, does not close `ERG-010`, and does not
approve public/security-product positioning.

It also does not approve production/security/compliance positioning, broader public distribution,
production deployment readiness, sandbox claims, EDR/MDM claims, SIEM custody claims, compliance
claims, compliance automation, legal advice, automated certification, regulatory-grade audit
claims, custody-grade audit claims, tamper-proof logging, audit immutability claims, production
identity, enterprise RBAC, runtime Postgres, hosted telemetry, hosted MCP, remote MCP, managed
model serving, support/deployment/update/incident-response claims, sandbox orchestration, local
model invocation, trusted-host promotion, SIEM adapter behavior, compliance mapping runtime
behavior, shell/Docker/Kubernetes/browser governed powers, arbitrary HTTP, broad filesystem writes,
plugin SDK behavior, or any new governed tool power.

## Reviewer Question

The reviewer should decide whether the current no-go claim boundary, forbidden claim categories,
allowed local-preview wording, and required preconditions for a future claim-specific decision
record are complete enough to keep `ERG-010` blocked while later claim-decision drafting is prepared.

Only a later committed decision record may move `ERG-010` away from `blocked`, and public/security-
product positioning remains blocked until explicitly approved by that future process.

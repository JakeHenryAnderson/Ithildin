# v0.1 Release Evidence

This document preserves the corrected local release evidence for the v0.1 public-preview review.
It is not a substitute for rerunning the commands before tagging; it records the known-good evidence
that was supplied for external review on 2026-05-29.

## Corrected Command Context

- Correct repository root: `/Users/jake/Documents/Codex/Ithildin`
- Correct command sequence:
  - `cd /Users/jake/Documents/Codex/Ithildin`
  - `make release-check`
  - `make release-evidence`
- An earlier `make release-check` was accidentally run from another repository named `palimpsest`.
  That failed with `No rule to make target 'release-check'` and is not Ithildin release evidence.

## Corrected Release Check Summary

The corrected `make release-check` run from `/Users/jake/Documents/Codex/Ithildin` passed:

- manifest lock check passed;
- public-preview release guardrails passed;
- Python tests passed: `211 passed`;
- Ruff lint passed;
- mypy passed for `56 source files`;
- UI TypeScript typecheck passed;
- docs site generation passed;
- UI production build passed with Vite.

## Corrected Evidence Snapshot Summary

The corrected `make release-evidence` run reported:

- commit: `7e1cb5d79002b08db38ce9e84bec0f88fe8e546c`;
- git dirty state: `false`;
- manifest lock current: `true`;
- tool count: `10`;
- audit chain valid: `true`;
- audit event count: `24`;
- runtime storage backend: SQLite;
- Postgres runtime enabled: `false`;
- telemetry enabled: `false`;
- dev admin token active: `false`;
- remote MCP enabled: `false`;
- CORS wildcard allowed: `false`;
- Compose local API/UI publish evidence: `127.0.0.1:8000` and `127.0.0.1:5173`;
- known deferred boundaries include production identity, runtime Postgres, hosted telemetry, remote
  MCP, shell execution, Docker socket access, Kubernetes tools, browser automation, arbitrary HTTP,
  broad filesystem writes, plugin SDK/marketplace, and cryptographic signing/notarization.

## Current Evidence Command

`make release-evidence` now includes repo-root and project-marker evidence and fails closed when it
is not run from the Ithildin repository root. Required markers are:

- `pyproject.toml`;
- `Makefile`;
- `apps/api`;
- `apps/mcp-server`;
- `tool-manifests.lock.json`.

Rerun this before tagging:

```sh
cd /Users/jake/Documents/Codex/Ithildin
make release-check
make release-evidence
```

The generated JSON is the authoritative final evidence for the commit being tagged.

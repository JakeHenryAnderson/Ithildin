# v0.6 Internal Review Execution Wave 2

This report records the internal high-intelligence subagent source-review execution pass for the
remaining v0.6 Wave 2 lanes. It is an internal pressure-test artifact only. It does not close
external/source review rows, does not approve public preview, and does not approve new governed tool
powers.

The run intentionally stopped at findings discovery because several high-severity issues were found.
Per the v0.6 boundary charter, these findings must be remediated or formally dispositioned before
later v0.6 closure, packet, or public-preview decision work continues.

## Scope

| Lane | Reviewer label | Source access | Result |
| --- | --- | --- | --- |
| Audit and signed evidence | Curie | internal source-level | High findings found; remediation required. |
| Policy parity and registry | Singer | internal source-level | High finding found; remediation required. |
| MCP ingress | Euler | internal source-level | Medium finding found; remediation required before closure. |
| Review console/admin boundary | Mencius | internal source-level | High finding found; remediation required. |
| Release automation/evidence gates | Huygens | internal source-level | High finding found; remediation required. |

Previously completed internal review lanes for patch apply, filesystem/platform, and HTTP fetch are
recorded in [v0.6-internal-subagent-review-wave.md](v0.6-internal-subagent-review-wave.md). Those
findings were remediated in the internal checkpoint `Remediate internal subagent findings`.

## Finding Queue

The finding IDs below are reserved for the next remediation sprint. They are not external findings
and they do not close external review rows.

| Reserved ID | Severity | Lane | Summary | Recommended next action |
| --- | --- | --- | --- | --- |
| SUB-010 | High | Audit/signed evidence | Signed exports can be produced while audit lifecycle diagnostics report SQLite/JSONL drift. | Gate signed export on clean lifecycle diagnostics or embed/verify lifecycle status. |
| SUB-011 | High | Audit/signed evidence | Signed bundle verification can return top-level `valid: true` while embedded audit verification is false. | Split signature validity from audit validity; make top-level validity require both. |
| SUB-012 | Medium | Audit/signed evidence | Non-object JSON audit payloads can crash verification paths instead of returning structured invalid results. | Require decoded JSON objects in SQLite and exported JSONL verification. |
| SUB-013 | Medium | Audit/signed evidence | Audit export endpoints do not write `audit.exported` events. | Add safe export audit events for plain and signed exports. |
| SUB-014 | Low | Audit/signed evidence | Signed-evidence demo verifier ignores artifact hashes recorded in `summary.json`. | Verify recorded artifact path, byte count, and SHA-256. |
| SUB-015 | High | Policy/registry | Filesystem resources are marked `in_scope: true` whenever `path` exists, so preview/policy can allow traversal that executors later deny. | Derive filesystem/workspace scope before policy using executor-equivalent path safety. |
| SUB-016 | Medium | Policy/registry | Manifest `input_schema` is not validated as a JSON Schema at registry load; malformed schemas can raise request-time `SchemaError`. | Validate schemas at manifest load and catch `SchemaError` defensively. |
| SUB-017 | Medium | Policy/registry | Policy parity harness can miss executor denial masking a false preview/runtime policy allow. | Require runtime status/resource-scope expectations for parity cases and add out-of-scope filesystem fixtures. |
| SUB-018 | Medium | MCP ingress | MCP `tools/call` does not enforce `manifest.mcp.exposed`, while `tools/list` does. | Add call-path exposure gate and tests for unexposed registered tools. |
| SUB-019 | High | Review console/admin | `/approvals/review` can report valid approval evidence when runtime binding checks would reject policy/principal/proposal-status drift. | Mirror runtime binding checks in approval review and disable approval on invalid evidence. |
| SUB-020 | Medium | Review console/admin | System Trust UI omits principal/workspace/storage/telemetry posture and labels audit-only status as `Verified`. | Render omitted posture fields and rename audit-only badge. |
| SUB-021 | Low | Review console/admin | Approve/deny routes accept a body `decision` field but ignore route/body mismatches. | Reject decision mismatches with safe client errors. |
| SUB-022 | High | Release automation | `review-packet-diff-gate` trusts `artifact-hashes.json` without recomputing artifact hashes or detecting unlisted files. | Recompute hashes, fail on missing/mismatched/unlisted artifacts, and scan for secrets. |
| SUB-023 | Medium | Release automation | Review bundle `.json` artifacts are command transcripts, not raw JSON. | Emit raw validated JSON plus sidecar transcript metadata. |
| SUB-024 | Medium | Release automation | Release evidence validation does not bind passing transcript claims to an existing same-commit transcript. | Add strict handoff validation for transcript existence, return code, status, and commit. |
| SUB-025 | Medium | Release automation | v0.6 finding namespaces in docs disagree with normalizer/reviewer validators. | Align docs and validators on a single namespace convention. |
| SUB-026 | Low | Release automation | `release-check` can mutate generated finding-summary outputs instead of failing on stale artifacts. | Add compare-only/check mode for release gates. |

## Remediation Status

The audit/signed-evidence lane has been remediated and converted into fixed structured finding
records:

- `SUB-010`: runtime signed exports now require a clean audit lifecycle and export metadata includes
  lifecycle diagnostics.
- `SUB-011`: offline signed-bundle verification now requires both signature validity and embedded
  audit-chain validity.
- `SUB-012`: SQLite and exported JSONL verification now reject non-object JSON payloads with
  structured invalid results.
- `SUB-013`: plain and signed export endpoints now write safe `audit.exported` events.
- `SUB-014`: the signed-evidence demo verifier now checks recorded artifact path, byte count, and
  SHA-256 metadata.

The policy/registry lane has also been remediated and converted into fixed structured finding
records:

- `SUB-015`: filesystem resources now derive scope through executor-equivalent workspace path
  checks before policy evaluation.
- `SUB-016`: manifest input schemas are validated as JSON Schema at registry load and request-time
  schema errors fail safely.
- `SUB-017`: policy parity now includes resource type/scope expectations and an out-of-scope
  filesystem denial fixture.

The MCP ingress and review-console/admin lanes have also been remediated and converted into fixed
structured finding records:

- `SUB-018`: MCP `tools/call` now denies registered tools that are not exposed through manifest MCP
  metadata.
- `SUB-019`: approval review now checks policy version, matched rules, requesting principal, and
  proposal status alongside the existing binding checks.
- `SUB-020`: the review console now shows principal, workspace, storage, and telemetry posture and
  labels audit-only verification as `Audit chain OK`.
- `SUB-021`: approval mutation routes now reject route/body decision mismatches.

The release automation/evidence gate lane has also been remediated and converted into fixed
structured finding records:

- `SUB-022`: review packet diff gates now recompute listed artifact hashes, detect unlisted
  artifacts, and scan packet contents for secret-like markers.
- `SUB-023`: review bundle `.json` artifacts are now raw JSON, with command transcript metadata in
  adjacent sidecar files.
- `SUB-024`: release evidence validation now binds passing attached release-check transcript claims
  to a readable same-commit transcript and successful return-code evidence.
- `SUB-025`: reviewer finding validators now accept the documented v0.6 namespace forms.
- `SUB-026`: release gates now check generated finding summaries without mutating tracked outputs;
  explicit regeneration uses `make review-findings-summary-write`.

All Wave 2 internal-review findings have been remediated for local-preview review purposes.
External/source review remains pending.

## Patch-Apply Recheck Pilot

The follow-up proxy-review operating model is recorded in
[v0.6-internal-proxy-review-operating-model.md](v0.6-internal-proxy-review-operating-model.md).
Its first patch-apply recheck found `SUB-027`, an approval expiry transition race after the
external-finding remediation. The process/evidence review also found `SUB-028` through `SUB-031`,
covering external-response lane binding, explicit no-findings markers, packet-hash validation, and
remediation traceability.

All five findings have been converted into structured finding records and fixed internally. External
source review remains pending.

## HTTP Fetch Recheck

The HTTP fetch proxy recheck added `SUB-040` through `SUB-047` on top of the earlier `SUB-001` and
`SUB-007` through `SUB-009` remediation trail. The recheck fixed malformed URL resource redaction,
preview/runtime validation order, raw Unicode URL handling, JSON parser edge handling, dispatch
packet finding coverage, closure traceability, lane-result documentation, and a stale HTTP contract
link.

Focused checks used for the recheck include:

- `uv run pytest tests/test_http_tools.py tests/test_governed_tool_calls.py tests/test_policy_parity.py -q`
- `uv run python scripts/policy_parity.py --json`
- `make release-check`

This is internal proxy evidence only. The HTTP fetch row remains external/source-review pending.

## Signed Evidence Recheck

The signed-evidence proxy recheck added `SUB-048` through `SUB-063` on top of the earlier
`SUB-010` through `SUB-014` remediation trail. The recheck fixed failed signed-export event
ordering, nested signed-export metadata verification, trusted-public-key wording, non-object bundle
handling, corrupt audit payload list diagnostics, manifest-lock signature status failures,
release-evidence signed-lock validation, startup/current signed-lock drift evidence, untrusted key
ID reporting, custom demo lock-path verification, closure traceability, dispatch pointers, stale
artifact regeneration requirements, durable signed-demo verification transcripts, release-evidence
dispatch commands, and explicit release-check return-code validation.

Focused checks used for the recheck include:

- `uv run pytest tests/test_audit_writer.py tests/test_signed_evidence_demo.py tests/test_tool_registry.py tests/test_api_service.py tests/test_release_readiness.py -q`
- `make signed-evidence-demo`
- `make signed-evidence-demo-verify`
- `make release-evidence`
- `make release-check`

This is internal proxy evidence only. The signed evidence, audit integrity, manifest-lock
verification, and release-evidence rows remain external/source-review pending.

## Policy/Registry Recheck

The policy/registry proxy recheck added `SUB-064` through `SUB-073` on top of the earlier
`SUB-015` through `SUB-017` remediation trail. The recheck fixed explicit empty-principal preview
handling, real patch-apply parity service coverage, validation-error value redaction,
pre-policy-denial evidence clarity, strict workspace-registry fail-closed behavior, current
manifest-lock drift status, duplicate YAML key rejection, policy/registry closure traceability,
dispatch packet source/command coverage, and lane-level result evidence.

Focused checks used for the recheck include:

- `uv run pytest tests/test_policy_parity.py tests/test_policy_test_harness.py tests/test_policy_impact.py tests/test_policy_evaluator.py tests/test_tool_registry.py tests/test_identity.py tests/test_workspaces.py tests/test_api_service.py tests/test_governed_tool_calls.py -q`
- `make policy-test`
- `make policy-parity`
- `make release-check`

This is internal proxy evidence only. The policy parity, policy preview/impact, and registry
fail-closed rows remain external/source-review pending.

## MCP Ingress Recheck

The MCP ingress proxy recheck added `SUB-074` on top of the earlier `SUB-018` remediation trail.
The recheck fixed the evidence gap for registered tools that are intentionally not exposed over MCP:
they are still denied before execution, and the denial now goes through the shared governed-call
audit path with safe `deny_source: mcp_exposure` metadata.

Focused checks used for the recheck include:

- `uv run pytest tests/test_mcp_adapter.py tests/test_mcp_integration_flow.py tests/test_governed_tool_calls.py -q`
- `make release-check`

This is internal proxy evidence only. The MCP ingress row remains external/source-review pending.

## Review Console/Admin Recheck

The review console/admin proxy recheck added `SUB-075` on top of the earlier `SUB-019` through
`SUB-021` remediation trail. The recheck fixed copied evidence parity for approval reviews: the UI
still shows grouped binding evidence and disables approval when review checks fail, and its copy
action now includes the same derived review verdict/checks/reasons for reviewer comparison.

Focused checks used for the recheck include:

- `npm run typecheck --prefix apps/ui`
- `npm run build --prefix apps/ui`
- `uv run pytest tests/test_api_service.py::test_approval_review_endpoint_reports_binding_checks tests/test_api_service.py::test_approval_review_endpoint_reports_runtime_binding_drift tests/test_api_service.py::test_approval_mutation_routes_reject_body_decision_mismatch -q`
- `make release-check`

This is internal proxy evidence only. The review-console/admin rows remain external/source-review
pending.

## Release Automation Recheck

The release automation proxy recheck added `SUB-076` on top of the earlier `SUB-022` through
`SUB-026`, `SUB-054`, and `SUB-060` through `SUB-063` remediation trail. The recheck fixed focused
dispatch coverage for release/evidence automation so reviewers get specific scripts, tests, docs,
findings, and required commands instead of broad directory pointers.

Focused checks used for the recheck include:

- `make release-evidence`
- `make release-evidence-validate`
- `make release-evidence-gate`
- `make packet-redaction-scan`
- `make v06-review-dispatch-packets`
- `make release-check`

This is internal proxy evidence only. The release automation rows remain external/source-review
pending.

## Tests Reported By Reviewers

- Audit/signed evidence: `uv run pytest tests/test_audit_writer.py tests/test_audit_diagnostics.py tests/test_signed_evidence_demo.py tests/test_tool_registry.py` passed.
- Policy/registry: policy, parity, OPA, registry, identity, workspace, governed-call, and API focused pytest suites passed; follow-up recheck also passed the focused pytest command plus `make policy-test` and `make policy-parity`.
- MCP ingress: `uv run pytest tests/test_mcp_adapter.py tests/test_mcp_integration_flow.py tests/test_governed_tool_calls.py` passed; `no_new_powers_guardrail` passed.
- Review console/admin: focused approval review/governed-call tests passed; `npm run typecheck --prefix apps/ui` passed.
- Release automation: `uv run pytest tests/test_release_readiness.py -q` passed.

## Stop Decision

The original Wave 2 review stopped on high-severity evidence and review-boundary findings. The
remediation follow-up has now addressed the original `SUB-010` through `SUB-026` queue and the
later signed-evidence recheck findings `SUB-048` through `SUB-063`, and the later policy/registry
recheck findings `SUB-064` through `SUB-073`, plus MCP ingress finding `SUB-074`, without adding
new runtime powers. The later review-console/admin recheck added `SUB-075`, also without adding new
runtime powers. The later release-automation recheck added `SUB-076`, also without adding new
runtime powers.

External/source review remains pending. Internal subagent review is useful pressure testing, not an
external substitute.

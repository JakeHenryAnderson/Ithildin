# v0.7 Patch Apply Recheck Request

Task 219 prepares the first v0.7 external-review closure lane. It asks an external/source reviewer
to recheck the patch-apply lane after the `EXT-PA-001` through `EXT-PA-004` fixes. This is recheck
preparation only: it does not close the lane without an external response.

## Current Boundary

- External handoff: go.
- External/source review closure: incomplete.
- Capability expansion: no-go.
- Public/security-product positioning: no-go.
- No new governed tool powers.

Patch apply remains Ithildin's only local-preview write path and remains stored-proposal-only.
Shell, Docker, Kubernetes, browser automation, broad filesystem writes, production identity, runtime
Postgres, hosted telemetry, remote MCP, plugin SDKs, arbitrary HTTP, and public/security-product
positioning remain deferred.

## Findings For Recheck

| Finding | Severity | Status | Recheck focus |
| --- | --- | --- | --- |
| `EXT-PA-001` | medium | fixed | Completion audit failure after file replacement should leave recoverable attempt evidence. |
| `EXT-PA-002` | medium | fixed | Proposal-level compare-and-set should prevent duplicate application through two approvals. |
| `EXT-PA-003` | low | fixed | Approval begin-execution transition should include expiry guard. |
| `EXT-PA-004` | low | fixed | Unified-diff hunk old/new counts should be enforced. |

The structured records are:

- [ext-pa-001-final-completion-audit-state.md](findings/ext-pa-001-final-completion-audit-state.md)
- [ext-pa-002-proposal-single-use-cas.md](findings/ext-pa-002-proposal-single-use-cas.md)
- [ext-pa-003-approval-expiry-transition.md](findings/ext-pa-003-approval-expiry-transition.md)
- [ext-pa-004-unified-diff-hunk-counts.md](findings/ext-pa-004-unified-diff-hunk-counts.md)

## Packet Generation

Generate the minimal patch-apply recheck packet from a clean worktree:

```bash
make v06-patch-apply-review-packet
```

The output is ignored local evidence under:

```text
var/review-packets/v0.6/patch-apply-external-review/
```

The packet includes:

- source bundle for `patches.py`, `approvals.py`, and `tool_calls.py`;
- focused tests for patch proposals, approvals, governed calls, and security regressions;
- the patch-apply checklist, state machine, filesystem contract, source pointers, and this recheck
  request;
- artifact hashes for all markdown packet attachments.

## Verification Commands

Run focused tests before sending the recheck packet:

```bash
uv run pytest tests/test_patch_proposals.py tests/test_approval_workflow.py tests/test_governed_tool_calls.py tests/test_security_regressions.py
make reviewer-findings-check
make review-findings-summary
make external-review-closure-gate
make release-check
```

`make external-review-closure-gate` is expected to remain valid but incomplete until the external
recheck response is normalized and ingested.

## Required Reviewer Response

Ask the reviewer to answer:

- whether `EXT-PA-001` through `EXT-PA-004` are closed for the local-preview patch-apply lane;
- whether any new `EXT-PA-###` finding is required;
- whether any finding is critical/high and should stop unrelated work;
- whether residual medium/low risks should be fixed before public preview or accepted-deferred.

The reviewer should use the `EXT-PA-###` namespace and cite source files/functions for
implementation claims.

## Closure Rule

Do not mark patch apply `closed_local_preview` until the external recheck response is normalized,
finding records are updated, focused verification commands pass, and the source-review closure
matrix is updated. Internal fixes and passing tests are necessary but not sufficient for closure.

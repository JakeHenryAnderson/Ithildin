# v0.6 Internal Proxy Review Operating Model

This document records the operating model for using high-intelligence subagents as a repeatable
internal proxy review bench while reserving GPT 5.5 Pro / Very High or a human expert for major
v0.X milestone consultation.

Internal proxy review is pressure testing. It is not external closure, not a substitute for
independent implementation review, and not permission to add new governed tool powers.

## Purpose

The goal is to reduce handoff latency while preserving Ithildin's safety posture:

1. automate routine source/evidence review;
2. catch implementation drift before a milestone packet leaves the repo;
3. turn findings into structured `SUB-###` records;
4. reserve external consultation for boundaries, high-risk findings, and v0.X go/no-go decisions.

## Lane Loop

For each focused lane:

1. Choose a narrow surface: patch apply, filesystem/platform, HTTP fetch, signed evidence, policy
   parity/registry, MCP ingress, review console/admin, or release automation.
2. Run the focused tests and packet/evidence command for that surface.
3. Spawn two or three high-intelligence subagents with disjoint scopes and source-file pointers.
4. Require each subagent to answer with judgment, evidence, residual risk, and structured findings.
5. Convert actionable results into `SUB-###` finding files.
6. Remediate high/medium findings or explicitly defer lower findings with accepted-risk rationale.
7. Run focused tests, then `make release-check`.
8. Update the closure matrix and lane notes as internal proxy reviewed, never externally closed.
9. Continue only when the tree is clean and the lane has no open critical/high internal finding.

## Stop Conditions

Stop autonomous implementation and report status when any of these happen:

- a critical or high internal finding appears;
- subagents disagree on a security boundary or product claim;
- the same focused test failure repeats after three fix attempts;
- a fix requires broadening the tool surface, MCP transport, identity model, storage backend,
  telemetry posture, or filesystem/network/write power;
- evidence cannot be made deterministic or secret-free;
- the repo cannot return to a clean verified checkpoint;
- the current issue is architectural rather than implementation-local.

## External Review Cadence

Use GPT 5.5 Pro / Very High or a human expert at these gates:

- after all v0.6 review lanes are internally proxy-clean;
- before any v0.X public-preview, broader positioning, or capability-expansion decision;
- before adding any new powerful tool class;
- after any critical/high internal finding, even if remediated;
- when a boundary, threat-model, identity, storage, telemetry, or MCP transport decision is unclear.

Purely mechanical docs/evidence cleanup does not need external consultation unless it changes claims.

## Current v0.6 Lane Order

1. Patch apply recheck.
2. Filesystem and platform semantics.
3. HTTP fetch canonicalization and SSRF controls.
4. Audit and locally signed evidence.
5. Policy parity and registry fail-closed behavior.
6. MCP ingress.
7. Review console and admin boundary.
8. Release automation and evidence gates.

## Pilot Result

The first patch-apply proxy recheck caught `SUB-027`, an approval expiry transition race after the
previous external-finding remediation. The process/evidence reviewer also caught `SUB-028` through
`SUB-031`, which tightened external response intake and remediation traceability.

That result supports the strategy: subagents are useful as fast internal reviewers, but their
findings must still be recorded, fixed, and kept distinct from external/source closure.

## Filesystem Lane Result

The filesystem/platform lane recheck found `SUB-032` through `SUB-039`. The findings covered
descriptor-based no-follow read opening, Unicode control path filtering, symlink handling in
list/search traversal, release-evidence filesystem capability validation, filesystem support
capability gating, patch-output binary denial, pre-replace stale-base rechecking, and safe invalid
Unicode patch denial.

All eight findings have been converted into structured finding records and fixed internally. The
filesystem closure row remains external-pending.

## HTTP Lane Result

The HTTP fetch lane recheck covered URL parsing, exact allowlist behavior, DNS/IP validation,
redirect revalidation, proxy suppression, response bounds, safe errors, policy-preview/runtime
parity, and review packet traceability.

The lane includes historical HTTP findings `SUB-001`, `SUB-007`, `SUB-008`, and `SUB-009`, plus the
current recheck findings `SUB-040` through `SUB-047`. The current findings covered malformed URL
resource redaction, preview schema/resource ordering, raw Unicode URL handling, JSON parser edge
errors, dispatch packet finding coverage, closure traceability, lane-level result evidence, and a
stale contract link.

Focused verification for the lane includes:

- `uv run pytest tests/test_http_tools.py tests/test_governed_tool_calls.py tests/test_policy_parity.py -q`
- `uv run python scripts/policy_parity.py --json`
- `make release-check`

Residual risk remains: internal proxy review is not an external/source review, and `http.fetch`
still requires focused external inspection before any broader network capability or stronger public
security claim.

## Signed Evidence Lane Result

The audit and locally signed evidence lane recheck covered signed audit export lifecycle semantics,
offline bundle verification, manifest-lock signature status, release-evidence signed-lock
validation, demo evidence verification, and dispatch packet traceability.

The lane includes historical signed-evidence findings `SUB-010` through `SUB-014`, plus the current
recheck findings `SUB-048` through `SUB-063`. The current findings covered failed-export success
evidence, nested metadata validation, trusted-public-key wording, non-object bundle handling,
corrupt audit list diagnostics, manifest-lock signature status, required signed-lock evidence,
startup/current signature drift, untrusted key IDs, custom demo lock paths, closure traceability,
dispatch pointers, stale generated artifacts, demo verification transcripts, release-evidence
commands, and explicit release-check return-code evidence.

Focused verification for the lane includes:

- `uv run pytest tests/test_audit_writer.py tests/test_signed_evidence_demo.py tests/test_tool_registry.py tests/test_api_service.py tests/test_release_readiness.py -q`
- `make signed-evidence-demo`
- `make signed-evidence-demo-verify`
- `make release-evidence`
- `make release-check`

Residual risk remains: this is local Ed25519 evidence only, not external notarization, hosted
custody, or official supply-chain signing. The signed-evidence rows remain external/source-review
pending.

## Policy/Registry Lane Result

The policy/registry lane recheck covered preview/runtime parity, trusted principal resolution,
resource construction, manifest schema validation, principal and workspace registry strictness,
manifest-lock drift visibility, YAML parser behavior, and dispatch packet traceability.

The lane includes historical policy/registry findings `SUB-015` through `SUB-017`, plus the current
recheck findings `SUB-064` through `SUB-073`. The current findings covered explicit empty-principal
preview denial, real patch-apply parity service wiring, validation-error redaction, pre-policy
denial evidence, strict workspace-registry fail-closed behavior, current manifest-lock drift
reporting, duplicate YAML key rejection, closure traceability, dispatch packet coverage, and
lane-level result evidence.

Focused verification for the lane includes:

- `uv run pytest tests/test_policy_parity.py tests/test_policy_test_harness.py tests/test_policy_impact.py tests/test_policy_evaluator.py tests/test_tool_registry.py tests/test_identity.py tests/test_workspaces.py tests/test_api_service.py tests/test_governed_tool_calls.py -q`
- `make policy-test`
- `make policy-parity`
- `make release-check`

Residual risk remains: policy/registry behavior is internally proxy-reviewed only. External/source
review is still required before treating the policy/registry rows as closed.

## MCP Ingress Lane Result

The MCP ingress lane recheck covered stdio adapter thinness, fixed local MCP principal/session use,
MCP-exposed listing, call-path exposure enforcement, unknown-tool denial evidence, and dispatch
packet traceability.

The lane includes historical MCP finding `SUB-018` plus follow-up finding `SUB-074`. `SUB-074`
found that registered-but-unexposed MCP calls were denied safely but did not leave shared audit
evidence. The remediation routes those denials through `GovernedToolCallService.deny_tool_call`,
recording safe `policy.evaluated` denial metadata with `deny_source: mcp_exposure`.

Focused verification for the lane includes:

- `uv run pytest tests/test_mcp_adapter.py tests/test_mcp_integration_flow.py tests/test_governed_tool_calls.py -q`
- `make release-check`

Residual risk remains: MCP ingress behavior is internally proxy-reviewed only. External/source
review is still required before treating the MCP ingress rows as closed.

## Review Console/Admin Lane Result

The review console/admin lane recheck covered local admin token handling, approval review evidence,
approval mutation routes, system trust posture, patch diagnostics display, and dispatch packet
traceability.

The lane includes historical review-console/admin findings `SUB-019` through `SUB-021`, plus
follow-up findings `SUB-075`, `SUB-078` through `SUB-080`, and `SUB-084`. `SUB-075` found that copied approval
evidence omitted the derived review verdict/checks/reasons that control approval enablement.
`SUB-078` found that the UI gated approve actions on fetched review evidence, but the API did not
re-run patch-apply binding review immediately before marking approvals approved. `SUB-079` found
that patch-apply diagnostics exposed safe details through the API but rendered only counts in the
console. `SUB-080` was later closed by the v0.8 dedicated frontend Vitest/React Testing Library
interaction-test harness.
`SUB-084` found that malformed patch-apply approvals missing `proposal_id` could still be approved,
even though runtime patch apply failed closed.
The remediations add safe copied review summaries, server-side approval-time binding review for
patch apply including missing-scope rejection, and content-free diagnostic detail tables without
adding raw diffs, file contents, secrets, new repair APIs, or new mutation controls.

Focused verification for the lane includes:

- `npm run typecheck --prefix apps/ui`
- `npm run build --prefix apps/ui`
- `uv run pytest tests/test_api_service.py::test_approval_review_endpoint_reports_binding_checks tests/test_api_service.py::test_approval_review_endpoint_reports_runtime_binding_drift tests/test_api_service.py::test_approve_patch_apply_rejects_stale_binding_review tests/test_api_service.py::test_patch_apply_approval_requires_valid_binding_scope_before_approval tests/test_api_service.py::test_approval_mutation_routes_reject_body_decision_mismatch -q`
- `make release-check`

Residual risk remains: review-console/admin behavior is internally proxy-reviewed only.
External/source review is still required before treating the review-console rows as closed.

## Release Automation Lane Result

The release automation lane recheck covered release evidence, packet hashing, generated review
bundle freshness, redaction scanning, no-new-powers gates, external-review closure gates,
capability-decision blocking behavior, and dispatch packet traceability.

The lane includes historical release-automation findings `SUB-022` through `SUB-026`, plus related
signed-evidence/release findings `SUB-054` and `SUB-060` through `SUB-063`, and follow-up findings
`SUB-076`, `SUB-077`, and `SUB-081` through `SUB-086`. `SUB-076` found that the release-automation
dispatch packet used broad directory references and incomplete finding coverage. `SUB-077` found
that `make review-candidate` did not refresh the focused v0.6 dispatch packets. `SUB-081` found
that the reviewer artifact manifest omitted the v0.6 dispatch artifacts. `SUB-082` found stale
dispatch pointers to renamed policy/registry finding docs. `SUB-083` found that the source-review
transcript skeleton lacked a release-automation section. `SUB-085` found that the focused
release-automation dispatch/source-pointer inventory omitted central packet-generation scripts.
`SUB-086` found that the transcript-packet documentation was stale after the dedicated release
automation section was added. The remediation gives external reviewers focused script, test,
document, command, and finding pointers; refreshes dispatch packets during the one-command handoff;
validates dispatch source/doc pointers; and aligns release-automation transcript coverage in both
generated output and docs.

Focused verification for the lane includes:

- `make release-evidence`
- `make release-evidence-validate`
- `make release-evidence-gate`
- `make packet-redaction-scan`
- `make v06-review-dispatch-packets`
- `make source-review-transcript-packet`
- `make reviewer-artifact-manifest`
- `make review-candidate`
- `make release-check`

Residual risk remains: release automation is internally proxy-reviewed only. External/source review
is still required before treating release/evidence automation rows as closed.

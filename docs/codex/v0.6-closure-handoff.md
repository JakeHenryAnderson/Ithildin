# v0.6 Closure Handoff

This note records the current v0.6 handoff state after internal proxy review, remediation, and
review-candidate regeneration. It is a handoff aid only. It does not close external/source review
and does not approve capability expansion.

**Current status:** v0.6/v0.7 external-review closure work for the v0.1 local-preview runtime
boundary; some generated paths retain historical v0.2 names.

## Current State

- Runtime boundary: unchanged v0.1 local-preview mediation boundary.
- Current handoff purpose: v0.6 external/source review execution and closure.
- Latest clean remediation baseline: structured internal findings `SUB-001` through `SUB-086`
  are fixed in committed finding records, except `SUB-080`, which is explicitly deferred as a low
  frontend interaction-test harness assurance item.
- Review packet command: `make review-candidate`, which now refreshes focused v0.6 dispatch
  packets before regenerating the main bundle and consolidated packet.
- Lane status command: `make v06-lane-status`.
- Lane status board: [v0.6-lane-status-board.md](v0.6-lane-status-board.md).
- Consolidated packet command: `make review-packet-consolidated`.
- Consolidated packet path:
  `var/review-packets/v0.2/GPT-5.5-Pro-consolidated/`.

The generated packet path still uses the historical `v0.2` directory because the packet generator
predates later review waves. The current contents are v0.6 handoff material for the same v0.1
local-preview runtime boundary.

## What Changed Since the Last External Packet Review

- Signed audit/export evidence now fails closed on audit lifecycle drift, embeds lifecycle
  diagnostics, and treats embedded audit verification failure as top-level signed-bundle invalidity.
- Audit export endpoints now write `audit.exported` events, and non-object audit payloads fail
  verification structurally instead of crashing verifier paths.
- Filesystem policy resources now derive `in_scope` through executor-equivalent workspace path
  validation before policy/preview decisions.
- Tool manifest input schemas are validated as JSON Schema at registry load.
- Policy parity fixtures now include resource type and scope expectations.
- MCP `tools/call` now enforces manifest MCP exposure instead of relying only on `tools/list`.
- Approval review now mirrors runtime binding checks for policy version, matched rules, requesting
  principal, proposal status, and existing proposal/manifest/schema fields.
- Review console trust posture now includes principal, workspace, storage, telemetry, and clearer
  audit-chain wording.
- Approval mutation routes reject contradictory route/body decisions.
- Review packet diff gates now recompute artifact hashes, detect unlisted packet files, and scan
  generated packet contents for secret-like markers.
- Review bundle `.json` files are raw JSON with sidecar command transcripts.
- Release evidence validation now binds passing transcript claims to a readable same-commit
  transcript and successful return-code evidence.
- Finding namespace validation and finding-summary check-mode are aligned with the documented
  v0.6 review process.
- Policy/registry proxy recheck now covers explicit empty-principal denial, real patch-apply parity
  service wiring, validation-error redaction, pre-policy denial evidence, strict workspace-registry
  behavior, current manifest drift reporting, duplicate YAML key rejection, and dispatch
  traceability.
- MCP ingress recheck now ensures registered-but-unexposed tool calls are denied through shared
  denial evidence instead of silently ending in the adapter.
- Review-console recheck now includes the derived approval-review verdict/checks/reasons in copied
  local evidence so reviewers can compare copied evidence with the visible approve/attention state.
- Release-automation recheck now gives reviewers focused source/test/doc/finding pointers instead
  of broad directory-level dispatch references.
- Review-candidate automation now refreshes focused v0.6 dispatch packets as part of the
  one-command handoff, so lane-specific packets cannot silently remain stale after bundle
  regeneration.

## Current Gate Results

The regenerated review candidate should show:

- `make release-check` passed.
- `make test` passes; the exact test count is recorded by the latest release-check transcript.
- Ruff, mypy, UI typecheck, docs-site build, and UI production build passed.
- `reviewer_findings.py` found no open critical/high findings.
- Tool count remains `10`.
- No new governed tool powers were added.
- Capability expansion remains blocked.
- Closed/reference lanes for the v0.1 local-preview boundary: patch apply, filesystem/platform,
  HTTP fetch, signed evidence/audit/manifest-lock verification, policy/registry, and MCP ingress.
- Source-level review received but pending intake/gates: none in the current packet. Any newer
  response supplied separately must remain pending closure until normalized and gated.
- Still pending source review: review console/admin, release/evidence automation, accepted-risk
  disposition, public-preview decision, capability-decision rows, and broader closure rows.

## What Remains Open

- Review console/admin, release/evidence automation, accepted-risk disposition, public-preview
  decision, capability-decision rows, and broader closure rows remain open.
- Any lane with source-level review received but not yet normalized through the intake workflow
  remains pending closure until gates pass.
- Accepted risks remain local-preview scoped and external-review pending.
- Capability expansion remains blocked until external review closure, accepted-risk disposition, and
  a future explicit capability decision.
- Public/security-product positioning remains blocked.
- Production identity, runtime Postgres, hosted telemetry, remote MCP hosting, shell/Docker/
  Kubernetes/browser tools, arbitrary HTTP, broad filesystem writes, plugin SDKs, and hosted trust
  roots remain deferred.

## Reviewer Ask

Reviewers should decide whether this remediated v0.6 packet is coherent and ready for the remaining
source-level external review execution, not whether Ithildin is production-ready.

Closed/reference lanes:

- patch apply approval binding, state transitions, and failure evidence;
- filesystem/platform workspace path and platform-support behavior;
- HTTP fetch canonicalization, redirect, DNS/IP, proxy, timeout, and safe-error behavior;
- signed audit export, audit integrity, and signed manifest-lock verification;
- policy/registry parity and fail-closed local trusted configuration;
- stdio MCP ingress thinness and exposure gates.

Remaining highest-value review areas:

- review-console approval evidence and admin trust boundaries;
- release automation integrity and packet evidence semantics.

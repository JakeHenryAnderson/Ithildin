# v0.6 Medium-Risk Disposition

Task 195 records the current medium/lower-risk disposition posture. This document does not close
external/source review rows.

## Current Disposition

- Patch-apply external findings `EXT-PA-001` and `EXT-PA-002` were medium severity and are fixed in
  structured finding records, then externally rechecked as closed for local-preview patch apply.
- Patch-apply external findings `EXT-PA-003` and `EXT-PA-004` were lower-severity follow-ups and are
  fixed in structured finding records, then externally rechecked as closed for local-preview patch
  apply.
- Internal `SUB-*` medium findings are fixed internally, but they remain internal pressure-test
  evidence, not external closure.
- Remaining lanes without external source review stay `external_pending`.

## Rule

Medium findings may be fixed or accepted-deferred only with a structured finding record,
verification notes, and closure-matrix evidence. Acceptance does not approve capability expansion
or broader product claims.

## Verification

```bash
make reviewer-findings-check
make review-findings-summary
make v06-lane-status
```

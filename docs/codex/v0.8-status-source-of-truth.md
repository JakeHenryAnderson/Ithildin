# v0.8 Status Source Of Truth

Task v0.8 Wave 1 records the current product-risk status after the v0.6/v0.7 focused
source-review lane closure work.

**Current status:** v0.8 roadmap/product-risk consultation after v0.6/v0.7 focused
source-review lane closure for the v0.1 local-preview runtime boundary.

This document is the source of truth for v0.8 roadmap/product-risk planning. Historical `v0.2`
packet paths and v0.6/v0.7 document names remain as lineage, but they do not change the current
status.

## Current Truth Table

| Area | Current status | Decision |
| --- | --- | --- |
| Focused implementation lanes | closed for v0.1 local preview | reference-only closure |
| Accepted-risk rows | dispositioned | closed local-preview or accepted-deferred in v0.8 |
| Product-decision rows | decisions recorded | final v0.8 packet records explicit go/no-go decisions |
| Limited technical-preview sharing | conditional_go | allowed only with warning packet and local-preview framing |
| Public/security-product positioning | blocked | no-go |
| Capability implementation | blocked | no new governed tool powers |
| Capability design | conditional_go | design-only proposals and risk analysis only |
| Production identity/storage/telemetry/remote MCP | deferred | not approved |

## Closed Reference Lanes

The focused lanes closed for the v0.1 local-preview runtime boundary are patch apply,
filesystem/platform, HTTP fetch, signed evidence/audit/manifest-lock verification,
policy/registry, MCP ingress, review console/admin, and release/evidence automation.

These closures do not approve public/security-product positioning, capability implementation,
production identity, runtime Postgres, hosted telemetry, remote MCP, shell/Docker/Kubernetes/browser
tools, arbitrary HTTP, broad filesystem writes, plugin SDKs, hosted trust roots, custody-grade
evidence, or external notarization.

## Final v0.8 Decisions

- Accepted risks have explicit `closed_local_preview` or `accepted_deferred` disposition.
- Public-preview claims have an explicit go/no-go decision.
- Capability design has a separate design-only decision from capability implementation.
- `SUB-080` is fixed with local-preview UI interaction-test assurance.
- The final v0.8 packet states decisions directly rather than inferring them from task completion.

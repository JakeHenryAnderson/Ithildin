# Ithildin Demo Workspace

This workspace is intentionally small and safe to mutate during local demos.

Try these flows through the review console or an MCP client:

- preview policy for `fs.list` with `{}`;
- read this file with `fs.read`;
- propose a one-line patch to this file;
- approve and apply the stored patch;
- verify and export the audit log.

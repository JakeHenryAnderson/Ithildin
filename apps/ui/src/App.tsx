import "./styles.css";

export function App() {
  return (
    <main className="shell">
      <section className="intro">
        <p className="eyebrow">Governed MCP gateway</p>
        <h1>Ithildin</h1>
        <p className="summary">
          Local-first tooling for policy-scoped, auditable agent actions.
        </p>
      </section>
    </main>
  );
}

name: http.fetch
version: 0.1.0
title: Fetch an allowlisted HTTP URL
risk: network
category: network
mcp:
  exposed: true
  annotations:
    readOnlyHint: true
input_schema:
  type: object
  additionalProperties: false
  required:
    - url
  properties:
    url:
      type: string
      minLength: 1
